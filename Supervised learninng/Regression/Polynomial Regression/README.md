# Polynomial Regression

This folder contains custom implementations of polynomial regression curves.

## File Registry & Audit

*   **`plr.py`**: Generates a design matrix with polynomial terms up to degree 17, standardizes the features to prevent exploding values, and runs gradient descent.
